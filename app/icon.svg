<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" role="img" aria-label="LinkedOut icon">
  <style>
    :root { --tile: #0A66C2; --fg: #FFFFFF; }
    @media (prefers-color-scheme: dark) {
      :root { --tile: #0A66C2; --fg: #FFFFFF; }
    }
    .tile { fill: var(--tile); }
    .fg { fill: var(--fg); }
  </style>
  <!-- LinkedIn-style tile with 'out' set in white -->
  <rect x="2" y="2" width="20" height="20" rx="4" class="tile"/>
  <text x="12" y="13" text-anchor="middle" dominant-baseline="middle"
        class="fg" font-family="Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif"
        font-size="9" font-weight="800" letter-spacing="-0.3">
    out
  </text>
</svg>
